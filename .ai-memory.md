# AI Memory — Service Manager (Electron)

## Tech Stack
- Electron 28, Node.js, vanilla JS (no bundler)
- nodeIntegration: true, contextIsolation: false (renderer dùng require trực tiếp)
- electron-builder (NSIS target, Windows)

## Project Structure (sau refactor)
```
src/
  main/
    index.js       ← entry point Electron (createWindow, lifecycle, shortcuts)
    ipc.js         ← tất cả ipcMain.on handlers; SERVICE_CONFIGS để thêm service mới
    services.js    ← spawn/stop/update/getStatus; processes registry
    settings.js    ← load/save settings.json từ userData
    tray.js        ← Tray create/updateMenu/notify
  renderer/
    index.js       ← entry point renderer (init tất cả modules)
    state.js       ← runtime state { router, openclaw }
    ui.js          ← DOM helpers: setStatus, addLog, toast, metrics, alert, ripple...
    views/
      dashboard.js ← service cards, IPC handlers cho status/log/update
      logs.js      ← clear all logs
      settings.js  ← load/save settings UI
    components/
      logPanel.js       ← collapsible log + filter
      commandPalette.js ← Ctrl+K palette, extensible qua addCommand()
index.html         ← trỏ src/renderer/index.js
package.json       ← main: src/main/index.js
```

## Patterns
- SERVICE_CONFIGS trong ipc.js: thêm service mới chỉ cần thêm 1 entry
- commandPalette.addCommand() để đăng ký lệnh mới từ module khác
- Mỗi view là module độc lập export { init }
- ui.js là pure DOM helpers, không có state
- **Renderer require path**: index.html dùng `require(path.join(__dirname, 'src/renderer/index.js'))` để __dirname trong các module con resolve đúng
- **Settings**: settings object có `_save()` và `_settingsPath` (không serialize vào JSON), chỉ save các key trong DEFAULTS + windowBounds

## Services hiện tại
- 9Router: port 20128, cmd `9router`, npm pkg `9router`
- OpenClaw: port 18789, cmd `openclaw gateway`, npm pkg `openclaw`

## Settings path
- `%APPDATA%\claw-router-manager\settings.json`

## Follow-ups / Mở rộng
- Thêm service mới: thêm entry vào SERVICE_CONFIGS (ipc.js) + card HTML + state entry
- Thêm view mới: tạo src/renderer/views/myview.js, import trong renderer/index.js
- Thêm tiện ích máy tính: tạo src/renderer/views/tools/ và src/main/tools/

## Tools đã có
- **Hẹn giờ tắt máy** (`src/renderer/views/tools/shutdown.js` + `src/main/tools/shutdown.js`)
  - Tắt máy / khởi động lại sau thời gian tùy chọn
  - Countdown realtime + progress bar đổi màu (xanh → vàng → đỏ)
  - Preset nhanh: 5m, 10m, 15m, 30m, 1h, 2h
  - Hủy lệnh bất kỳ lúc nào
  - IPC: shutdown-schedule, shutdown-cancel, shutdown-now

- **Reset IDM Trial** (`src/renderer/views/tools/idm-reset.js` + `src/main/tools/idm-reset.js`)
  - Reset thời gian dùng thử của Internet Download Manager
  - Tự động kill IDM processes (IDMan.exe, IEMonitor.exe)
  - Xóa registry keys: ConfigTime, MData
  - Reset giá trị Thread=1, Model=0x68
  - Xóa folder %appdata%\IDM
  - Kiểm tra trạng thái IDM đang chạy hay không
  - IPC: idm-reset-trial (handle), idm-check-running (handle)

## Bug Fixes
- Button ripple no longer changes button text color on click.
  - Root cause: `.ripple` used `mix-blend-mode: overlay`, so the ripple blended with foreground text instead of only the button background.
  - Fix: removed the ripple blend mode, isolated `.btn`, and wrapped button content in `.btn-content` so text/icons stay above the ripple layer.
  - Files touched: `style.css`, `src/renderer/ui.js`
  - Remaining risk: future CSS that targets direct button children should account for the `.btn-content` wrapper.
- Native button active/focus styling could still override button text color during press-and-hold.
  - Root cause: `.btn` and `.btn-icon` still relied on browser default button appearance, so Chromium could apply native pressed/focus painting and text-selection highlight on top of custom styles.
  - Fix: reset appearance for `.btn`/`.btn-icon`, locked foreground color across `:active/:focus/:focus-visible`, disabled text selection for button content, and kept ripple constrained below the content layer.
  - Files touched: `style.css`, `src/renderer/ui.js`
  - Remaining risk: if new button variants are added, they should define `--btn-fg` so active/focus color locking stays correct.
- Rolled back the temporary System Metrics feature, including the dedicated renderer view, navigation tab, IPC endpoint, and service-side CPU/RAM sampling code.
- Added `autoHeal` to settings defaults plus settings UI save/load flow. Main IPC now exposes `set-auto-heal` and propagates the toggle to `src/main/services.js`.
- `src/main/services.js` now owns service registration hooks, restart orchestration, unresponsive detection (>30s), and auto-heal rate limiting (max 3 restarts/hour). Auto-heal emits service logs and tray notifications.
- Validation for this change used `node --check` on: `src/main/services.js`, `src/main/ipc.js`, `src/main/settings.js`, `src/renderer/index.js`, and `src/renderer/views/settings.js`.
- Brainstormed Windows-related feature ideas for Service Manager: startup impact monitor, service watchdog profile, Windows Update maintenance mode, network adapter quick reset, firewall rule manager, tray health snapshots, and event-log based crash insights. Prioritized practical path: Startup Impact Monitor + Maintenance Mode before deeper OS-level tools.
- Brainstormed advanced/special Windows features: True Windows Service Mode (system-level daemon), Desktop HUD Widget (transparent click-through overlay), Taskbar/JumpList Magic, Global Kill-Switch (Ninja Mode), and Network Traffic Radar. Awaiting user preference to proceed with Codex CLI.
- Added 10 more Windows-focused feature ideas (system restore point, dependency map, bandwidth throttler, auto-backup config, smart restart scheduler, etc.). Awaiting user selection.
- **Reset IDM/AnyDesk buttons không hoạt động (lần 2)**
  - Symptom: Click nút Kiểm tra / Reset không có phản hồi gì
  - Root cause: `saveBtn.addEventListener(...)` trong `idm-reset.js` — `saveBtn` không được khai báo (element không tồn tại trong HTML mới). `ReferenceError` throw ngay khi `init()` chạy, crash toàn bộ module trước khi bất kỳ event listener nào được bind.
  - Fix: Xóa đoạn `saveBtn.addEventListener` (element đã bị loại khỏi HTML khi redesign card), giữ lại `startScheduler(schedule, doReset)`.
  - Files touched: `src/renderer/views/tools/idm-reset.js`
  - Lesson learned: Khi redesign HTML và xóa element, phải đồng thời xóa tất cả JS reference đến element đó. `node --check` không phát hiện được `ReferenceError` runtime dạng này — cần kiểm tra thủ công hoặc dùng `node -e` để grep.
  - Remaining risk: Nếu thêm element mới vào HTML mà không có JS handler tương ứng (hoặc ngược lại), sẽ lại bị lỗi tương tự.

- **AnyDesk ID không lấy được + logo sai**
  - Root cause 1 (ID): `service.conf` không chứa `ad.anynet.id`. ID thực tế nằm trong `%PROGRAMDATA%\AnyDesk\system.conf` (dòng `ad.anynet.id=...`). Code cũ chỉ đọc `%APPDATA%\AnyDesk\service.conf` → không tìm thấy.
  - Root cause 2 (logo): File download về bị sai extension — `anydesk-logo.png` thực ra là ICO 16x16, `idm-logo.png` thực ra là JPEG. ICO 16x16 quá nhỏ và không render tốt trong `<img>`.
  - Fix: Đọc `%PROGRAMDATA%\AnyDesk\system.conf` cho ID. Dùng SVG inline cho AnyDesk logo (không phụ thuộc download). Đổi idm-logo thành `.jpg` đúng extension.
  - Files touched: `src/main/tools/anydesk-reset.js`, `src/renderer/views/tools/anydesk-reset.js`, `src/renderer/views/tools/idm-reset.js`, `src/renderer/assets/`
  - Lesson learned: Luôn kiểm tra magic bytes khi download file binary. AnyDesk version mới lưu ID trong `system.conf` tại `%PROGRAMDATA%`, không phải `service.conf` tại `%APPDATA%`.
- Added two new persisted tool datasets inside the existing settings storage pattern: `prompts[]` and `links[]` in `%APPDATA%\\claw-router-manager\\settings.json`.
- Main IPC now exposes CRUD-style handlers for tool data:
  - `library-get-all`
  - `prompt-save`, `prompt-delete`
  - `link-save`, `link-delete`, `link-toggle-read`
- Added renderer views:
  - `src/renderer/views/tools/prompts.js`
  - `src/renderer/views/tools/links.js`
  Both are self-contained modules, loaded via `src/renderer/index.js`, matching the existing per-view architecture.
- Reset IDM UI was reframed into a broader `Reset` workspace in `index.html` + `style.css`:
  - nav label changed from `Reset IDM` to `Reset`
  - IDM reset actions were compacted into a single primary card
  - a secondary placeholder card was added to reserve space for future reset tools without changing layout again
- Added reusable UI patterns for future lightweight CRUD tools in `style.css`:
  - `library-layout`, `library-form-card`, `library-list-card`
  - `library-item`, `library-empty`, `library-actions`
  - `reset-layout`, `reset-card`, `reset-extension-card`
- Prompt tool UX:
  - create, edit, delete, copy
  - empty state and toast feedback
  - timestamps shown from `createdAt` / `updatedAt`
- Link tool UX:
  - create, edit, delete, open link
  - quick toggle read/unread
  - sort modes: unread first, latest, name A-Z
  - unread badge/priority in UI
- Reset UI was tightened again so IDM now lives in one compact card:
  - removed the secondary extension placeholder from the Reset workspace
  - reduced icon, padding, status sizing, and step typography
  - replaced long copy with short summary pills and compact step labels
- Link storage now persists richer metadata in `%APPDATA%\\claw-router-manager\\settings.json`:
  - `pinned: boolean` on each link
  - `preview: { title, favicon, hostname, fetchedAt }` on each link
- Link UX now includes:
  - per-item star pin action with pinned items always sorted above unpinned items
  - pinned visual treatment in the list plus a `Pinned` indicator chip
  - preview row with favicon thumbnail and fetched page title under the custom link name
- `src/main/ipc.js` now fetches link preview metadata during `link-save` and fails soft so saving still works if the target page blocks or times out.
- Command palette now includes navigation shortcuts for `Prompt`, `Link`, and `Reset`.
- Validation for this change used `node --check` on:
  - `src/main/ipc.js`
  - `src/main/settings.js`
  - `src/renderer/index.js`
  - `src/renderer/components/commandPalette.js`
  - `src/renderer/views/tools/prompts.js`
  - `src/renderer/views/tools/links.js`
  - `src/renderer/views/tools/idm-reset.js`
- UI tối ưu Prompt và Link workspace (tháng 4/2026):
  - Xóa `library-card-head` (h2 + p mô tả) khỏi cả form card và list card — không thêm giá trị, chiếm không gian
  - Xóa `prompt-refresh-btn` khỏi header Prompt — ít dùng
  - Xóa badge "Prompt" trên mỗi item — redundant khi đã ở trong view Prompt
  - Rút gọn timestamp prompt: chỉ hiện `updatedAt`, bỏ `createdAt` riêng
  - Xóa `link-thumbnail` 120px khỏi mỗi link card — tốn không gian, favicon nhỏ bị stretch
  - Gộp badge read/unread vào title row thay vì hàng badge riêng
  - Gọn `library-list-tools`: bỏ label wrapper `library-sort`, select nằm thẳng trong flex row
  - Rút gọn text: "Hủy chỉnh sửa" → "Hủy", "Tự động phân loại" → "Tự động", "Xuất JSON/Nhập JSON" → "Xuất/Nhập"
  - `link-form-title` và `prompt-form-title` giờ là label của field thay vì h2 riêng
  - CSS: xóa `.library-card-head`, `.library-sort`, `#view-links .library-card-head` overrides; giảm `min-height` item từ 280px → auto; giảm gap item từ 14px → 10px
  - Files touched: `index.html`, `style.css`, `src/renderer/views/tools/prompts.js`, `src/renderer/views/tools/links.js`
- UI fixes sau phản hồi screenshot (tháng 4/2026):
  - Fix `.library-content` overflow: giảm `max-height` từ 220px → 120px, thêm `-webkit-line-clamp: 5` để cắt text dài
  - Tăng button size: `.btn-sm` padding từ 7px 12px → 8px 14px; `.btn-icon` từ 30px → 34px
  - Tăng visual hierarchy: `.library-item-head h3` font-size từ 15px → 16px, font-weight từ 600 → 700
  - Tăng `.library-actions` border-top từ 1px → 2px, padding-top từ 10px → 14px để tách biệt rõ hơn
  - Xóa `min-height: 280px` khỏi `.library-item` — không còn cần vì đã bỏ thumbnail
  - Improve empty state: thêm `.library-empty` CSS với icon SVG thay vì chữ cái đơn, padding 60px, icon 64px với border
  - Improve badge "Chưa đọc": tăng font-size từ 10px → 11px, padding từ 2px 7px → 3px 9px, font-weight 700
  - Thêm title tooltip cho action button edit/delete trong link card
  - Files touched: `style.css`, `src/renderer/views/tools/prompts.js`, `src/renderer/views/tools/links.js`
  - `settings.json` now persists a root-level `sync_url` alongside `prompts[]` and `links[]`.
  - `index.html` + `style.css` now include a `Sync Settings` button in the Link header and a modal for entering the public Google Sheets CSV URL.
  - `src/main/ipc.js` now exposes:
    - `link-sync-save` to validate and persist the CSV URL
    - `link-sync-now` to fetch the CSV, parse `name/url/pinned/read`, merge with local `settings.links`, and skip duplicates by normalized URL
  - `src/renderer/views/tools/links.js` now:
    - loads `sync_url` from `library-get-all`
    - uses `Làm mới` as a real sync action when a CSV URL is configured
    - saves the modal URL and immediately runs sync via `Save & Sync Now`
  - Sync import behavior:
    - local links win on URL collisions
    - new remote links are prepended before existing items
    - imported links get lightweight preview metadata (`title=name`, `hostname`, no favicon fetch)
- Validation for this change used `node --check` on:
  - `src/main/ipc.js`
  - `src/main/settings.js`
  - `src/renderer/views/tools/links.js`
- **Reset AnyDesk chạy step nhưng không có tác dụng thực tế**
  - Symptom: UI chạy đủ step Reset AnyDesk và thường báo thành công, nhưng AnyDesk ID/config trên Windows không đổi.
  - Root cause: flow renderer -> IPC -> main có thật, nhưng `src/main/tools/anydesk-reset.js` là implementation quá lỏng: chỉ `taskkill` `AnyDesk.exe`, không dừng AnyDesk Windows service/background process, chỉ đụng một vài path cố định, và nuốt lỗi ở `taskkill`, `unlinkSync`, restore nên gần như luôn trả `{ ok: true }` dù artifact ID vẫn còn. Điều này tạo false success ở UI.
  - Fix: thay tool bằng flow thật hơn và có verify hậu điều kiện: scan cả `%PROGRAMDATA%\\AnyDesk` và `%APPDATA%\\AnyDesk` cùng custom subdirs `ad_*`, backup `user.conf`, dừng process + Windows service AnyDesk, xóa `service.conf`/`system.conf`, restore `user.conf`, và fail nếu artifact ID vẫn còn. Thêm `dryRun` + step log để verify logic mà không tác động hệ thống.
  - Files touched: `src/main/tools/anydesk-reset.js`, `src/renderer/views/tools/anydesk-reset.js`, `index.html`
  - Validation: `node --check` trên `src/main/tools/anydesk-reset.js`, `src/renderer/views/tools/anydesk-reset.js`, `src/main/ipc.js`; `node -e` dry-run `resetAnyDeskId({ dryRun: true })` cho step log đầy đủ.
  - Remaining risk: một số bản AnyDesk custom/service có thể dùng service name hoặc config layout khác ngoài pattern `AnyDesk` / `ad_*`; khi đó step log sẽ expose artifact còn sót thay vì báo success giả.
- **UI vẫn hiển thị cũ sau khi fix code (tháng 4/2026)**
  - Symptom: App bị treo ở loading với error `Cannot read properties of null (reading 'addEventListener')` line 32 trong links.js
  - Root cause: Tôi đã sai khi cố restore về 2-column layout (form card + list card) trong khi cấu trúc modal pattern đã có sẵn và tối ưu hơn. HTML bị conflict vì tôi xóa modal elements (`link-open-modal-btn`, `link-modal-close-btn`, `link-modal`) nhưng JS vẫn reference đến chúng.
  - Fix: Restore HTML và JS về modal pattern gốc (đúng), chỉ apply UI fixes nhỏ: empty state icon SVG, badge "Chưa đọc" lớn hơn (11px, padding 3px 9px, font-weight 700)
  - Lesson learned: **Không được thay đổi architecture pattern khi chỉ được yêu cầu fix UI**. Modal pattern đã tối ưu cho bảo trì và phát triển. Chỉ nên apply visual fixes (size, color, icon) mà không đụng structure.
  - Files touched: `src/renderer/views/tools/links.js` (chỉ update render, không đụng init)
  - Remaining risk: CSS fixes (button size, border-top) vẫn apply được với modal pattern.

- **Link workspace: Column layout theo category với auto-categorization (tháng 4/2026)**
  - Requirement: User muốn links hiển thị theo cột dọc (vertical columns), mỗi category là một cột riêng. Auto-categorization cần phân loại dựa vào URL pattern (github.com→tool, sheets→sheet, etc.)
  - Implementation:
    * Thêm `autoCategorize(url)` function với URL pattern matching cho: sheet, tool, study, social, work, auto, other
    * Thêm `getCategoryLabel(category)` để map category key sang label tiếng Việt
    * Thêm `renderLinkCard(link)` helper để tránh duplicate code
    * Modified `render()` để group links theo category và render thành columns
    * Xóa category badge khỏi individual cards vì column header đã hiển thị category
  - CSS changes:
    * Đổi `.library-list` từ grid layout sang flex layout với `flex-direction: row`, `overflow-x: auto`
    * Thêm `.link-column` (flex column, min-width 300px)
    * Thêm `.link-column-title` (sticky header cho mỗi cột)
    * Thêm `.link-column-items` (flex column chứa cards)
    * Responsive: wrap columns trên màn hình nhỏ, stack vertical trên mobile
  - Files touched: `src/renderer/views/tools/links.js`, `style.css`
  - Validation: `node --check src/renderer/views/tools/links.js` passed
  - UX improvements (tháng 4/2026):
    * Click vào card (không phải button) sẽ mở link luôn - thêm fallback logic trong `handleListClick()`
    * **Tăng height của Prompt cards**: `max-height: 300px`, `min-height: 380px`, bỏ `-webkit-line-clamp` và thêm `overflow-y: auto` để scroll được nội dung dài
    * Thêm custom scrollbar cho `.library-content` trong Prompt view
  - Files touched: `src/renderer/views/tools/links.js`, `style.css`
  - Remaining work: Test filter dropdown vẫn hoạt động với column layout, verify sort modes (unread/latest/name) vẫn apply đúng trong từng column

- **Bug: Columns bị giới hạn width và không scroll được (tháng 4/2026) - FIXED**
  - Symptom: Columns hiển thị bị cắt, chỉ thấy 2 cột, không scroll ngang được. Cả Link và Prompt view đều bị.
  - Root cause 1: `.link-column` có `flex: 1` khiến columns bị stretch để fill container thay vì giữ `min-width: 300px`. Khi có nhiều columns, chúng bị ép nhỏ lại.
  - Root cause 2: Media query `@media (max-width: 860px)` vẫn set `grid-template-columns: 1fr` cho `.library-list`, conflict với flex layout mới.
  - **Root cause 3 (REAL ROOT CAUSE)**: `.library-layout` dùng grid `minmax(320px, 380px) 1fr` → Cột 2 (`.library-list-card`) bị giới hạn bởi `1fr` (chiếm phần còn lại của viewport) → `.library-list` bên trong không thể overflow ra ngoài parent. Link và Prompt view không có form card nên grid 2 cột không cần thiết.
  - Fix:
    * Đổi `.link-column` từ `flex: 1` sang `flex: 0 0 auto` để columns giữ được min-width và không bị stretch
    * Xóa `grid-template-columns: 1fr` khỏi media query 860px
    * Thêm custom scrollbar styling cho `.library-list::-webkit-scrollbar` (height 8px, rounded)
    * **Override `#view-links .library-layout` và `#view-prompts .library-layout` thành `display: block` để bỏ grid constraint**
    * **Set `#view-links .library-list-card` và `#view-prompts .library-list-card` width 100% để chiếm full viewport**
  - Files touched: `style.css`
  - Validation: `node --check` passed cho cả links.js và prompts.js
  - Lesson learned: 
    * Khi dùng flex layout với `overflow-x: auto`, phải set `flex: 0 0 auto` cho flex items để chúng không bị shrink
    * **Parent container với grid/flex constraint sẽ block overflow của children. Phải override parent layout để cho phép horizontal scroll**
    * Kiểm tra toàn bộ layout hierarchy từ root → parent → child khi debug overflow issues
    * Khi có nhiều views dùng chung layout pattern, phải fix cho tất cả views cùng lúc
  - Symptom: `sc` fail voi `Failed to stop service AnyDesk: [SC] OpenService FAILED 5: Access is denied.` khi reset AnyDesk tren Windows non-admin.
  - Root cause: nhanh stop service coi `sc query/stop` access denied la exception chung, khong parse rieng `FAILED 5`, khong cho phep user-scope fallback, va khong tra huong dan ro rang khi artifact trong `%PROGRAMDATA%\AnyDesk` con.
  - Fix: parse `OpenService FAILED 5` / `Access is denied` thanh `needsAdmin`, tiep tuc thu stop process + xu ly `%APPDATA%\AnyDesk`, va verify cuoi cung tra `ok:false` kem huong dan chay app bang Administrator neu service/system artifacts chua duoc xu ly. Renderer AnyDesk hien thi message admin-required ro hon.
  - Files touched: `src/main/tools/anydesk-reset.js`, `src/renderer/views/tools/anydesk-reset.js`
  - Validation: `node --check src/main/tools/anydesk-reset.js`; `node --check src/renderer/views/tools/anydesk-reset.js`
